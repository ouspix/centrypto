{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start RSSHub",
            "type": "shell",
            "command": "sudo service docker start && docker compose up -d",
            "options": {
                "cwd": "${workspaceFolder}/rsshub-selfhost"
            },
            "problemMatcher": []
        }
    ]
}